{
  "id": "1318-41cu6k-vector-p0-curve-aware-planar-geometry-kernel-int",
  "title": "Vector P0: curve-aware planar geometry kernel + interaction oracle harness (no behavior change)",
  "description": "Foundation for authentic Flash 8 merge-drawing vector core (approved re-architecture; curve-preserving + full selection). Build a new packages/core/src/engine/planar/ kernel: a half-edge PLANAR SUBDIVISION (vertices + half-edges + faces; each edge carries fillLeft/fillRight/lineStyle) over QUADRATIC-bezier-aware geometry. Provide: segment/segment, segment/curve, and curve/curve intersection; arrangement construction (insert an edge, split existing edges + the new edge at all intersection points); face extraction (the planar regions); point-in-face; and CURVE-PRESERVING splitting (when an edge is cut, keep true quadratics \u2014 subdivide the bezier at parameter t / carry curve params on half-edges; do NOT flatten to polylines). Snap coordinates to the TWIP grid (Flash works in twips) for numerically stable intersections; reuse the degeneracy-perturbation lessons from engine/eraser.ts. Add PlanarShape/half-edge types in engine/types.ts near Shape (additive; do not change Shape). NO user-facing behavior change yet (kernel only). ALSO stand up the interaction-oracle harness: Ruffle pixel + stage-canvas screenshot specs in apps/desktop/e2e for the canonical cases as .todo placeholders (red-over-blue cut; blue-over-blue union; line-across-fill-then-move-half; two-crossing-lines=4 segments; erase-across-shape-splits; partial-fill-click+move-leaves-hole). VERIFY: build + core tests green (only ~3 task-1207 fixtures acceptable); kernel unit tests \u2014 Euler invariant V-E+F, shoelace AREA CONSERVATION under union/cut, intersection counts, curve-preservation (a cut quadratic refits/round-trips within epsilon); golden-parity + self-determinism UNCHANGED (no doc/SWF change). ACQUIRE lock; commit incl task file; push rebase-retry (if .tasks/.counter conflicts keep origin's). Scope: new engine/planar/ + engine/types.ts (additive) + e2e oracle stubs (+kernel tests). This kernel is the whole bet \u2014 do not under-scope; if it needs splitting, ship the kernel core green and report what remains.",
  "status": "open",
  "effort": "extreme",
  "priority": "high",
  "created_at": "2026-06-20T05:16:49Z",
  "updated_at": "2026-06-20T05:16:49Z"
}
